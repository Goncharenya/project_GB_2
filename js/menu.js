let menu   = document.querySelector('.menu');
let burger = document.querySelector(('.menu-top__line'));


    menu.addEventListener("click", () => {
        if (menu.contains('_active')){
            console.log('yes!');
    }})
